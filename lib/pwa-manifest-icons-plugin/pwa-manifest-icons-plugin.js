/**
 * This plugin piggybacks html-webpack-plugin. It injects a <link> element to
 * the index.html generated by html-webpack-plugin.
 *
 *
 * */

const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

function PWAManifestIconsPlugin(options) {
  this.options = Object.assign(
    {
      pathTo192: 'src/img/favicon/android-chrome-192x192.png',
      pathTo512: 'src/img/favicon/android-chrome-512x512.png',
      outputManifestFilePath: 'manifest.json',
      outputImgDir: 'img',
    },
    options || {},
  );
}

PWAManifestIconsPlugin.prototype.apply = function(compiler) {
  // ========== //
  // IMAGE MOVE //
  // ========== //
  // Move the relevant images to the destination folder

  // =============== //
  // FILE GENERATION //
  // =============== //
  // Inject new image paths to the manifest.json template and generate the
  // updated manifest.json file.
};

module.exports = PWAManifestIconsPlugin;
